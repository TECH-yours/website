$(document).ready(function()
{
    $('.nav_link').removeClass('active');
    $('#nav_dashboard').addClass('active');

    // ajax('GET', '/api/coupons', {}, function(data) {
    //     $('#coupon_count').text(data.coupons.length);
    // });

    // ajax('GET', '/api/points', {}, function(data) {
    //     $('#point_count').text(data.points);
    // });

    // ajax('GET', '/api/orders', {}, function(a) {
    //     if (!a.error)
    //     {
    //         $('#orders').empty();
    //         for(const i in a.orders)
    //         {
    //             const _this = a.orders[i];
    //             $('#orders').append(`
    //                 <tr>
    //                     <td>${_this.id}</td>
    //                     <td>${moment(_this.created_at).format('YYYY-MM-DD HH:mm:ss')}</td>
    //                     <td>${_this.status}</td>
    //                     <td>${parseInt(_this.total)}</td>
    //                 </tr>
    //             `);
    //             if (i == 4)
    //                 break;
    //         }
    //     }
    // });
});